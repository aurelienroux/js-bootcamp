let tempConv = function (temp) {
  return (temp - 32) * 5 / 9
}

let result = tempConv(32)
let otherResult = tempConv(68)

console.log(result);
console.log(otherResult);

let age = 25

age = null

console.log(age);
